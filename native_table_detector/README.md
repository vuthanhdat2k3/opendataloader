# Native Table Detector

Production-ready detector-first pipeline for rotated tables:
- Stage 1: OpenDataLoader extraction
- Stage 2: native rotated-table detection and matching
- Stage 3: deskew + OCR patching
- Stage 4: merge into JSON/Markdown

Main code lives in `src/`.

